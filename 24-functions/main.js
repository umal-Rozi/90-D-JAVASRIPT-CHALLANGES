
// functins= define code once, and use it many times 
// to perform some code, call functins name.
// waxy so koobysaa code aad so cel celcelin layd

startprogarm();

function startprogarm() {
    let name="umaal";
    let age= 20;

    happyday(name, age);
}
function happyday(name, age) {
    console.log("happyday");
    console.log("happyday", name);
    console.log("happyday");
    console.log("happyday", age);
}
